@inherits LayoutComponentBase

<div class="flex min-h-screen">
    <div class="bg-gray-800 text-white w-64 p-4 space-y-2" style="@(showNav ? \"display:block\" : \"display:none\")" class:md\:block="true">
        <NavMenu />
    </div>

    <div class="flex-1">
        <header class="bg-gray-200 p-4 flex md:hidden">
            <button @onclick="ToggleNav" class="text-gray-700">☰</button>
        </header>
        <main class="p-4">
            @Body
        </main>
    </div>
</div>

@code {
    private bool showNav;
    private void ToggleNav() => showNav = !showNav;
}
